# Протокол зв'язку ALTEC AL808 / PC900

Довідник складено на основі `AL808CommsProtocol.pdf` (офіційна специфікація протоколу ALTEC AL808).
PC900 використовує той самий протокол, що й AL808.

---

## 1. Загальний опис

Протокол — текстовий (ASCII), працює за схемою **master–slave**: master (PC/ПЛК) завжди
ініціює обмін, прилад лише відповідає.

- **RS232** — один прилад, кабель до 15 м.
- **RS485** — до 31 приладу + 1 master, кабель до 1200 м, рекомендовано для промислових об'єктів.

### Формат кадру UART

| Параметр | Значення |
|---|---|
| Start bit | 1 |
| Data bits | 7 |
| Parity | even (парний) |
| Stop bit | 1 |
| Позначення | **7E1** |

### Швидкість передачі (Baud rate)

300, 600, 1200, 2400, 4800, 9600, 19200 бод.

---

## 2. Керуючі символи

Кожен елемент кадру — один ASCII-символ.

| Символ | HEX | Назва | Призначення |
|---|---|---|---|
| `<STX>` | `02` | Start of text | Початок блоку даних у відповіді приладу / у запиті на запис |
| `<ETX>` | `03` | End of text | Кінець блоку даних, входить у розрахунок BCC |
| `<EOT>` | `04` | End of transmission | Початок запиту master (і для читання, і для запису) |
| `<ENQ>` | `05` | Enquiry | Завершує запит на **читання** |
| `<ACK>` | `06` | Acknowledge | Позитивна відповідь: запис виконано успішно |
| `<NAK>` | `15` | Negative acknowledge | Негативна відповідь: запис не виконано |
| ` ` (пробіл) | `20` | Space | Заповнення в полі DATA |
| `-` | `2D` | Minus sign | Знак мінус у DATA |
| `.` | `2E` | Decimal point | Десяткова крапка в DATA |
| `>` | `3E` | Greater than | Префікс hex-слів (наприклад `>ABCD`) |

Цифри `0`–`9` кодуються як HEX `30`–`39`. Букви мнемонік передаються звичайним ASCII
(наприклад, `P` = `50`, `V` = `56`).

---

## 3. Адресація приладу

Адреса приладу складається з двох десяткових цифр — `ADR_H` (старша) і `ADR_L` (молодша).
Кожна цифра **дублюється двічі** в кадрі як засіб перевірки:

```
(ADR_H)(ADR_H)(ADR_L)(ADR_L)
```

Приклади:

| Адреса | ASCII-кадр | HEX |
|---|---|---|
| `01` | `0 0 1 1` | `30 30 31 31` |
| `12` | `1 1 2 2` | `31 31 32 32` |
| `43` | `4 4 3 3` | `34 34 33 33` |
| `53` | `5 5 3 3` | `35 35 33 33` |

Якщо в адресі виявлено помилку парності або формату — прилад **не відповідає** (мовчить).

---

## 4. Команда читання даних (Poll / Read)

**Напрямок:** master → прилад. Не змінює жодних параметрів.

### Формат запиту

```
[EOT] (ADR_H)(ADR_H)(ADR_L)(ADR_L) (C1)(C2) [ENQ]
```

| Поле | Опис |
|---|---|
| `C1`, `C2` | Два символи мнемоніки параметра, який читається (напр. `P`,`V` для PV) |

### Формат відповіді (успіх)

```
[STX] (C1)(C2) <DATA> [ETX] (BCC)
```

| Поле | Опис |
|---|---|
| `C1`, `C2` | Відлуння мнемоніки з запиту |
| `DATA` | Значення параметра у форматі, як на індикаторі приладу (наприклад `99.9`, `1.2`, `-999`, `>1234`) |
| `BCC` | Контрольна сума — XOR усіх символів **після STX (не включаючи STX) і включно з ETX** |

Якщо мнемоніка невалідна або кадр запиту некоректний — відповіді немає взагалі (не `NAK`).

`BCC` іноді випадково дорівнює `04` (`EOT`) — драйвер не повинен трактувати цей байт
як початок нового кадру/кінець сеансу.

### Приклад: читання `PV`, адреса 53

Master → прилад:

```
EOT  5  5  3  3  P  V  ENQ
04   35 35 33 33 50 56 05
```

Прилад → master:

```
STX  P  V  2  4  .  ETX BCC
02   50 56 32 34 2E 03  2D
```

(`DATA = "24."`, тобто `PV = 24.0`)

---

## 5. Команда запису даних (Select / Write)

**Напрямок:** master → прилад. Змінює значення параметра.

### Формат запиту

```
[EOT] (ADR_H)(ADR_H)(ADR_L)(ADR_L) [STX] (C1)(C2) <DATA> [ETX] (BCC)
```

`BCC` розраховується так само: XOR усіх символів після `STX` (не включаючи його),
включно з `ETX` — тобто `C1`, `C2`, усі символи `DATA` та `ETX`.

### Відповідь приладу

| Відповідь | HEX | Значення |
|---|---|---|
| `[ACK]` | `06` | Запис виконано успішно |
| `[NAK]` | `15` | Запис не виконано |
| *(немає відповіді)* | — | Помилка парності або формату адреси |

`NAK` повертається, якщо:

- `BCC` неправильний;
- параметр відсутній або не сконфігурований;
- параметр доступний **тільки для читання**;
- значення виходить за допустимі межі.

### Приклад: запис `SP = 450`, адреса 43

Master → прилад:

```
EOT  4  4  3  3  STX  S  L  4  5  0  ETX  BCC
04   34 34 33 33 02   53 4C 34 35 30 03   2D
```

Прилад → master:

```
ACK
06
```

---

## 6. Розрахунок BCC (Block Check Character)

```
BCC = C1 XOR C2 XOR DATA[0] XOR DATA[1] XOR ... XOR ETX
```

- `STX` у розрахунок **не входить**.
- Результат — один байт (ASCII-символ), що передається останнім у кадрі.

---

## 7. Формат поля DATA

Значення передається **у тому вигляді, як воно показане на індикаторі приладу**, а не як
бінарне число.

| Тип | Приклад | Використання |
|---|---|---|
| Число з десятковою крапкою | `99.9`, `1.2` | PV, SP, температури, параметри PID |
| Від'ємне число | `-999` | величини зі знаком |
| Переповнення / спецформат | `>1234` | коли значення виходить за межі формату |
| Hex-слово | `>0000`, `>0001`, `>ABCD` | статус-слова `SW`, `XS`, `OS` |

---

## 8. Таблиця команд (мнемонік параметрів)

`C1 C2` — два ASCII-символи мнемоніки, що передаються в кадрі запиту/відповіді.

| Мнемоніка | HEX (C1 C2) | Назва параметра | Доступ | Опис |
|---|---|---|---|---|
| `PV` | `50 56` | Process Value | тільки читання | Останнє виміряне значення (поточна вимірювана величина) |
| `OP` | `4F 50` | Output power (auto) | тільки читання | Потужність вихідного сигналу в автоматичному режимі, % |
| `SP` | `53 50` | Setpoint | тільки читання | Поточна робоча ставка (setpoint), яку використовує алгоритм регулювання |
| `SL` | `53 4C` | Local setpoint 1 | читання/запис | Локальна ставка 1 |
| `HA` | `48 41` | High alarm setpoint | читання/запис | Уставка аварії верхньої межі |
| `LA` | `4C 41` | Low alarm setpoint | читання/запис | Уставка аварії нижньої межі |
| `DA` | `44 41` | Deviation alarm setpoint | читання/запис | Уставка аварії відхилення від ставки |
| `XP` | `58 50` | Proportional band | читання/запис | Пропорційна смуга (PID) |
| `TI` | `54 49` | Integral time | читання/запис | Інтегральний час (PID) |
| `TD` | `54 44` | Derivative time | читання/запис | Диференціальний час (PID) |
| `HB` | `48 42` | High cutback | читання/запис | Обмеження перерегулювання зверху (overshoot inhibition) |
| `LB` | `4C 42` | Lower cutback | читання/запис | Обмеження перерегулювання знизу |
| `CH` | `43 48` | Cycle time 1 (output 1 - heat) | читання/запис | Період циклу вихідного сигналу нагріву |
| `CC` | `43 43` | Cycle time 2 (output 2 - cool) | читання/запис | Період циклу вихідного сигналу охолодження |
| `RG` | `52 47` | Relative cooling gain | читання/запис | Відносний коефіцієнт охолодження (потужність нагріву : потужність охолодження) |
| `HS` | `48 53` | Setpoint high limit | читання/запис | Верхня межа допустимої ставки |
| `LS` | `4C 53` | Setpoint low limit | читання/запис | Нижня межа допустимої ставки |
| `BP` | `42 50` | Sensor break power | читання/запис | Потужність виходу при обриві датчика |
| `HO` | `48 4F` | Heat (output 1) output limit | читання/запис | Ліміт потужності виходу нагріву |
| `SR` | `53 52` | Ramp-to-setpoint ramp rate | читання/запис | Швидкість рампу до ставки |
| `Hb` | `48 62` | Holdback | читання/запис | Смуга holdback програматора (пауза програми, якщо PV відхиляється від SV більше ніж на цю величину) |
| `Lc` | `4C 63` | Loop counter | читання/запис | Лічильник циклів (повторів) програми |
| `ch` | `63 68` | Pattern number (PTN) | читання/запис | Номер патерну `0…9`. Немає в PDF AL808; є в таблиці PC900. Підтверджено на ACHI IR6500 (COM1, 2026-09-18). **Не плутати** з `CH` (`43 48`) |
| `SE` | `53 45` | Current segment | тільки читання | Номер поточного сегмента програми; у IDLE = `0` |
| `r1` | `72 31` | Ramp rate 1 | читання/запис | Швидкість рампу сегмента 1 програми |
| `l1` | `6C 31` | Level 1 | читання/запис | Цільова ставка (рівень) сегмента 1 |
| `t1` | `74 31` | Dwell 1 | читання/запис | Витримка (тривалість) сегмента 1 |
| `r2` | `72 32` | Ramp rate 2 | читання/запис | Швидкість рампу сегмента 2 |
| `l2` | `6C 32` | Level 2 | читання/запис | Цільова ставка сегмента 2 |
| `t2` | `74 32` | Dwell 2 | читання/запис | Витримка сегмента 2 |
| `SW` | `53 57` | Status word (hexadecimal) | читання/запис (частково) | Основне статус-слово приладу, формат `>ABCD` (див. розділ 9) |
| `XS` | `58 53` | Extended status word | читання/запис | Керування автоналаштуванням PID (self-tuning), формат `>ABCD` |
| `OS` | `4F 53` | Optional status word | читання/запис | Керування виконанням програми (програматора), формат `>ABCD` |

### 8.1. Значення `XS` (self-tuning)

| Значення | Дія |
|---|---|
| `>0000` | Вимкнути автоналаштування (self-tuning) |
| `>0001` | Увімкнути автоналаштування (self-tuning) |

### 8.2. Значення `OS` (керування програмою)

| Значення | Дія |
|---|---|
| `>0000` | Зупинити програму (stop) |
| `>0002` | Запустити програму (run) |
| `>0003` | Призупинити програму (hold) |

---

## 9. Статус-слово `SW`

Формат даних: `>ABCD` — чотири hex-ніблі (кожен по 4 біти), від старшого `A` до молодшого `D`.
Тобто ніблі відповідають бітам так: `D` = біти 0–3, `C` = біти 4–7, `B` = біти 8–11, `A` = біти 12–15.

| Біт | Нібль | Функція | Доступ | Значення |
|---|---|---|---|---|
| 0 | D0 | Data format | читання/запис | `0` = Free, `1` = Fixed |
| 1 | D1 | Input fault | тільки читання | No / Yes |
| 2 | D2 | Barring key operation (блокування клавіш) | читання/запис | No / Yes |
| 3 | D3 | *(зарезервовано)* | — | — |
| 4 | C0 | *(зарезервовано)* | — | — |
| 5 | C1 | Modifying parameters by keys | читання/запис | No / Yes |
| 6 | C2 | Deviation alarm status | тільки читання | No / Yes |
| 7 | C3 | Deviation alarm condition | тільки читання | No / Yes |
| 8 | B0 | Lower limit alarm status | тільки читання | No / Yes |
| 9 | B1 | Lower limit alarm condition | тільки читання | No / Yes |
| 10 | B2 | Upper limit alarm status | тільки читання | No / Yes |
| 11 | B3 | Upper limit alarm condition | тільки читання | No / Yes |
| 12 | A0 | Alarm output | тільки читання | No / Yes |
| 13 | A1 | *(зарезервовано)* | — | — |
| 14 | A2 | *(зарезервовано)* | — | — |
| 15 | A3 | Auto / Manual | тільки читання | Auto / Manual |

---

## 10. Приклад повного алгоритму обміну

**Читання:**
1. Master надсилає `[EOT] ADR_H ADR_H ADR_L ADR_L C1 C2 [ENQ]`.
2. Прилад відповідає `[STX] C1 C2 DATA [ETX] BCC` (або мовчить при помилці).
3. Master перевіряє відлуння `C1 C2` та звіряє `BCC`.

**Запис:**
1. Master надсилає `[EOT] ADR_H ADR_H ADR_L ADR_L [STX] C1 C2 DATA [ETX] BCC`.
2. Прилад відповідає `[ACK]` (успіх), `[NAK]` (відмова) або мовчить (помилка адреси/парності).

---

---

## 11. Апаратні особливості станції ACHI IR6500 (BGA Rework Station)

Ця станція використовує вбудований контролер, сумісний із протоколом AL808/PC900,
але з кількома відмінностями від *генеричної* специфікації PC900
(`PC900_EN.pdf`). Підтверджено інструкцією виробника (ACHI IR6500 User Manual)
**та** емпіричним тестом на реальному приладі (COM1, адреса 1):

| Параметр | Генерична специфікація PC900 | ACHI IR6500 (факт) |
|---|---|---|
| Dwell Time (`t1..t8` / `d1..d8`) | хвилини, `0~9999 min` | **секунди**, `0~9999 sec` (ціле число) |
| PTN (номер патерну) | не задокументовано в PDF AL808 | `0~9` (10 патернів), мнемоніка **`ch`** (`63 68`), читання/запис. Підтверджено на COM1 2026-09-18: `ch=0..9` → `ACK` і зміна банку `r/l/t`; `ch=10` → `NAK`. У RUN/HOLD не змінюється |
| Ramp Rate (`r1..r8`) | одиниць/хв, `0.01~99.99` | **одиниць/СЕКУНДУ**, `0.01~99.99` (спростовано початкове припущення про хвилини — див. тест від 18.09.2026 нижче) |
| Target Setpoint (`l1..l8` / `L1..L8`) | в межах `SPL..SPK` | `0~230°C` (апаратна межа станції) |
| `Hb` (Holdback) | смуга відхилення PV/SV для паузи програми | інструкція описує спрощено як "макс. температура нагріву, 230°C, не змінювати" — але механіка (пауза рампи при `|PV-SV|>Hb`) підтверджена експериментально й відповідає класичному Holdback |
| COM-порт | будь-який | інструкція вимагає **COM1** (у нашому застосунку порт обирається вільно) |

**Спеціальне значення `END` для Ramp Rate:** замість числа в `r1..r8` можна
записати текст `END` — програма завершується, щойно доходить до цього
сегмента (наступні сегменти не виконуються). Приклади з інструкції виробника:

```
Lead (Sn63Pb37), PTN-1:
  r1=1  L1=85   d1=70
  r2=1  L2=150  d2=35
  r3=1  L3=185  d3=50
  r4=END  Hb=230

Lead-Free (Sn96.5Ag3Cu0.5), PTN-2:
  r1=1  L1=85   d1=60
  r2=1  L2=140  d2=45
  r3=1  L3=170  d3=25
  r4=1  L4=220  d4=50
  r5=END  Hb=230
```

**Підсумок емпіричного тесту (COM1, адреса 1, 2026-09-17):**
- Усі 8 сегментів (`r/l/t` 1..8) відповіли на читання й запис (`ACK`) — екстрапольовані
  мнемоніки `r3..r8/l3..l8/t3..t8` підтверджені робочими на реальному приладі.
- Запис `r1=30.00` (units/min) → зчитано назад `30.00` — точний round-trip.
- Запис `t1=0.5` (як частка хвилини) → зчитано назад `0.` (`0`) — прилад
  очікував ЦІЛІ СЕКУНДИ, дробову "хвилину" обрізав до `0`. Це і виявило
  описану вище відмінність в одиницях dwell.
- Під час `RUN` з `Hb=5`, `PV` лишалась незмінною (немає підключеного
  нагрівального навантаження на стенді) → після того як `SP` відійшла від
  `PV` більш ніж на `Hb`, прилад сам поставив програму на holdback-паузу —
  очікувана і безпечна поведінка.

**Виправлення: справжня одиниця Ramp Rate — units/СЕКУНДУ, не units/хвилину
(тест на реальному приладі, COM1, адреса 1, 2026-09-18):**
- Попередній висновок "`r1=30.00` (units/min) → зчитано назад `30.00`"
  підтверджував лише коректність ЗЧИТУВАННЯ РАНІШЕ ЗАПИСАНОГО ЧИСЛА
  (round-trip), а НЕ реальну швидкість зміни `SP` в часі — повний прогін
  рампи тоді не спостерігався (програму зупинив holdback).
- Низькотемпературний тест профілю (25→35→45°C, `r1=12`, `r2=9` — тобто
  UI-значення `0.2`/`0.15°C/с`, помножені на 60 за старою конвертацією)
  показав, що `SP` досягала цілі за **<1.5с** замість очікуваних 50-67с.
- Контрольний експеримент: записано СИРЕ (без конвертації) `r1=1.0`,
  ціль `SP+5°C`. Результат: `SP` змінювалась лінійно рівно **~1°C щосекунди**
  (`21→30°C` за ~9с). Це однозначно доводить: нативна одиниця `r1..r8` —
  **°C за СЕКУНДУ**, пряма передача 1:1 з UI-значенням (°C/с), без `×60`.
- Виправлено в `app/profile_controller.py` (`ramp_ui_to_native`/
  `ramp_native_to_ui` тепер ідентичні функції, як і для dwell) та
  в симуляторі `app/mock_al808.py`.
- **Наслідок для попередніх тестів:** усі записи Ramp Rate на реальний
  прилад до цього виправлення виконувались у **60 разів швидше**, ніж
  вводилось у UI (наприклад, введені "0.5°C/с" фактично ставали "30°C/с"
  на приладі).

---

**Джерело:** ALTEC AL808 Communication Protocol (`AL808CommsProtocol.pdf`);
ACHI IR6500 User Manual (`d0fbbd.pdf`); емпіричний тест на реальному приладі.
