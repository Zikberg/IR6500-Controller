# Протокол зв’язку ALTEC AL808 / PC900

Документ зібрано з `AL808CommsProtocol.pdf` та інструкції `PC900_EN.pdf`.  
Протокол ASCII, master–slave. PC900 використовує **той самий** протокол, що AL808.

Перевірено на COM1 (CH340): **9600 бод, 7E1, адреса 01**.  
Читання `PV` повернуло `  22.` з коректним BCC. Запису в прилад не виконувалось.

---

## 1. Фізичний рівень

| Параметр | Значення |
|---|---|
| Інтерфейс | RS232 (1 прилад, до 15 м) або RS485 (до 31 приладу + master, до 1200 м) |
| Формат кадру UART | 1 start bit, **7 data bits**, **even parity**, 1 stop bit (**7E1**) |
| Швидкість | 300, 600, 1200, 2400, 4800, **9600**, 19200 бод |
| На PC900 | параметри `ADDR` (адреса) і `BAUD` у меню конфігурації (пароль `LOC = 808`) |

PC900 також може мати RS422 (4 проводи). Індикатор **COM** блимає під час активного обміну.

---

## 2. Керуючі символи

Кожен елемент кадру — **один ASCII-символ**.

| Символ | HEX | Назва | Призначення |
|---|---|---|---|
| `<STX>` | `02` | Start of text | Початок блоку даних (відповідь або запис) |
| `<ETX>` | `03` | End of text | Кінець блоку даних, входить у BCC |
| `<EOT>` | `04` | End of transmission | Початок запиту master |
| `<ENQ>` | `05` | Enquiry | Кінець запиту на **читання** |
| `<ACK>` | `06` | Acknowledge | Запис успішний |
| `<NAK>` | `15` | Negative acknowledge | Запис невдалий |
| ` ` (пробіл) | `20` | Space | Заповнення в DATA |
| `-` | `2D` | Minus | Знак мінус у DATA |
| `.` | `2E` | Decimal point | Десяткова крапка в DATA |
| `>` | `3E` | Greater than | Префікс hex-слів (`>ABCD`) |

Цифри `0`–`9` = HEX `30`–`39`. Літери мнемонік — звичайний ASCII (`P`=`50`, `V`=`56` тощо).

---

## 3. Команди протоколу

У протоколі **дві** команди: читання (poll) і запис (select).  
Параметр задається **двосимвольною мнемонікою** `C1 C2` (наприклад `PV`, `SL`, `OS`).

Адреса приладу — два десяткові символи `ADR_H` і `ADR_L`. Кожен символ **дублюється** для перевірки.

Приклади адреси:

| Адреса | Кадр адреси (ASCII) | HEX |
|---|---|---|
| `01` | `0 0 1 1` | `30 30 31 31` |
| `12` | `1 1 2 2` | `31 31 32 32` |
| `43` | `4 4 3 3` | `34 34 33 33` |
| `53` | `5 5 3 3` | `35 35 33 33` |

Якщо помилка парності або формату адреси — прилад **мовчить**.

---

### 3.1. Читання параметра (poll)

**Напрямок:** master → прилад. Нічого не змінює. Безпечно для перевірки зв’язку.

```
[EOT] (ADR_H) (ADR_H) (ADR_L) (ADR_L) (C1) (C2) [ENQ]
```

**Успішна відповідь приладу:**

```
[STX] (C1) (C2) <DATA> [ETX] (BCC)
```

| Поле | Опис |
|---|---|
| `C1 C2` | Відлуння мнемоніки з запиту |
| `DATA` | Значення в форматі індикатора: `99.9`, `1.2`, `-999`, `>1234`, з пробілами зліва |
| `BCC` | XOR усіх байтів **після STX включно з ETX** (сам STX не входить) |

BCC **може випадково дорівнювати** `EOT` (`04`). Драйвер не повинен трактувати цей байт як кінець сеансу.

**Якщо мнемоніка невалідна або кадр поганий** — відповіді немає (не `NAK`).

#### Приклад зі специфікації: читання PV, адреса 53

Master:

```
EOT  5  5  3  3  P  V  ENQ
04   35 35 33 33 50 56 05
```

Прилад (формат відповіді): `[STX] PV <значення> [ETX] BCC`.

#### Перевірений приклад (наш стенд, адреса 01, 9600 7E1)

Запит:

```
04 30 30 31 31 50 56 05
EOT 0  0  1  1  P  V  ENQ
```

Відповідь:

```
02 50 56 20 20 32 32 2E 03 2B
STX P  V     2  2  .  ETX BCC
```

`DATA` = `  22.` (PV ≈ 22).  
BCC: `50⊕56⊕20⊕20⊕32⊕32⊕2E⊕03 = 2B` (`+`) — збігається.

---

### 3.2. Запис параметра (select)

**Напрямок:** master → прилад. Змінює параметр. Використовувати лише свідомо.

```
[EOT] (ADR_H) (ADR_H) (ADR_L) (ADR_L) [STX] (C1) (C2) <DATA> [ETX] (BCC)
```

BCC рахується так само: XOR усіх символів після `STX` включно з `ETX`  
(тобто `C1`, `C2`, усі символи `DATA`, `ETX`).

**Відповідь:**

| Відповідь | HEX | Значення |
|---|---|---|
| `[ACK]` | `06` | Запис успішний |
| `[NAK]` | `15` | Запис невдалий |
| *(тиша)* | — | Помилка парності або формату адреси |

`NAK` буває, якщо:

- BCC неправильний;
- параметр відсутній / не сконфігурований;
- параметр **тільки для читання**;
- значення поза допустимими межами.

#### Приклад зі специфікації: запис `SL = 450`, адреса 43

Master:

```
EOT  4  4  3  3  STX  S  L  4  5  0  ETX  BCC
04   34 34 33 33 02   53 4C 34 35 30 03   2D
```

BCC: `53⊕4C⊕34⊕35⊕30⊕03 = 2D` (`-`).

Прилад при успіху: `ACK` (`06`).

---

### 3.3. Розрахунок BCC

```
BCC = C1 XOR C2 XOR DATA[0] XOR DATA[1] XOR ... XOR ETX
```

`STX` у BCC **не входить**. Результат — один байт (7-бітний ASCII після парності на лінії).

---

### 3.4. Формат DATA

Значення передається **як на індикаторі**, не як бінарне число.

| Тип | Приклад | Де використовується |
|---|---|---|
| Число з крапкою | `99.9`, `  22.`, `1.2` | PV, SP, температури, PID |
| Знак | `-999` | від’ємні величини |
| Переповнення / спец. | `>1234` | формат `>` + цифри |
| Hex-слово | `>0000`, `>0001`, `>ABCD` | `SW`, `XS`, `OS` |

Пробіли зліва — норма. Писати треба в тому ж стилі, який прилад показує при читанні.

---

## 4. Список параметрів (мнемоніки)

`C1 C2` — коди в кадрі. Колонка «На панелі» — позначення з інструкції PC900 / AL808.

### 4.1. Процес іставка

| C1 C2 | HEX | На панелі | Доступ | Опис |
|---|---|---|---|---|
| `PV` | `50 56` | PV | **тільки читання** | Останнє виміряне значення (process value). Безпечна перевірка зв’язку. |
| `OP` | `4F 50` | MV / вихід | **тільки читання** | Потужність виходу в автоматичному режимі, %. |
| `SP` | `53 50` | SP | **тільки читання** | Поточна ставка (setpoint), яку бачить алгоритм. Під час програми це **робоча** SV, не база `SP` простою. |
| `SL` | `53 4C` | локальна SP | читання / запис | Локальна ставка 1. Саме її пишуть, щоб змінити завдання в режимі константної температури. |

### 4.2. Аварії

| C1 C2 | HEX | На панелі | Доступ | Опис |
|---|---|---|---|---|
| `HA` | `48 41` | `HiAL` / `AL1` high | читання / запис | Уставка аварії верхньої межі (full-scale high). |
| `LA` | `4C 41` | `LOAL` | читання / запис | Уставка аварії нижньої межі (full-scale low). |
| `DA` | `44 41` | `DAL` | читання / запис | Уставка аварії відхилення від ставки (deviation). |

Типи аварій на PC900 задаються в конфігурації (`ALO1` / `ALO2`): `HiAL`, `LOAL`, `HDA`, `LDA`, `DAO`, `NDAO`, а також аварія зупинки програми. Через протокол змінюються **числа уставок**, не режим аварії.

### 4.3. PID і виходи

| C1 C2 | HEX | На панелі | Доступ | Опис |
|---|---|---|---|---|
| `XP` | `58 50` | `PROP` | читання / запис | Пропорційна смуга. Занадто вузька → коливання як ON/OFF; широка → повільно і зі зсувом. Якщо `CTRL = ON.OF`, на панелі це гістерезис. |
| `TI` | `54 49` | `INT.T` | читання / запис | Інтегральний час. `OFF` або `1…8000` с. Має бути довшим за час реакції об’єкта. |
| `TD` | `54 44` | `DER.T` | читання / запис | Диференціальний час. `OFF` або `1…999` с. Типово ≈ 1/6 від `INT.T`. |
| `HB` | `48 42` | `HcB` / `KCB` | читання / запис | High cutback — обмеження перерегулювання зверху (overshoot inhibition). **Не плутати** з `Hb` (holdback програми). |
| `LB` | `4C 42` | `LcB` / `LCB` | читання / запис | Lower cutback — те саме знизу. |
| `CH` | `43 48` | `Hc.T` | читання / запис | Період циклу виходу 1 (нагрів). Для контакторів ~20 с, для логіки/тиристора ~1 с. Зникає, якщо `CTRL = ON.OF`. |
| `CC` | `43 43` | `Cc.T` | читання / запис | Період циклу виходу 2 (охолодження). |
| `RG` | `52 47` | `REL.C` | читання / запис | Відносний коефіцієнт охолодження = потужність нагріву : потужність охолодження. Задати **до** self-tune. |
| `HO` | `48 4F` | `HPL` / `KPL` | читання / запис | Ліміт виходу 1 (heat), зазвичай `0…100` %. У ручному режимі вихід також обмежений цим параметром. |

### 4.4. Межі, датчик, рамп

| C1 C2 | HEX | На панелі | Доступ | Опис |
|---|---|---|---|---|
| `HS` | `48 53` | `SPK` / `SP H` | читання / запис | Верхня межа ставки. Завжди > `LS`. |
| `LS` | `4C 53` | `SPL` / `SP L` | читання / запис | Нижня межа ставки. Завжди < `HS`. Діапазон `SL` і рівнів програми обмежений цими межами. |
| `BP` | `42 50` | `SNBP` | читання / запис | Потужність при обриві датчика. Нагрів: `0…100` %; heat/cool: `-99.9…100` %. При обриві контур розмикається, індикатор `MAN` блимає, на дисплеї `SNB` або `OR`. |
| `SR` | `53 52` | `SPRR` | читання / запис | Швидкість рампу до ставки, одиниці/хв (`0.01…99.99`). Активний, якщо `CTRL = RSP`. |

### 4.5. Програматор (setpoint profile)

На PC900 з’являються, якщо `CTRL = P9`.  
У PDF явно наведені сегменти 1 і 2. Сегменти 3+ у таблиці протоколу не розписані; на панелі їх до 8 пар (16 сегментів: ramp + dwell).

**Важливо:** holdback і loop counter — **регістр нижнього регістру** в другому символі.

| C1 C2 | HEX | На панелі | Доступ | Опис |
|---|---|---|---|---|
| `Hb` | `48 62` | `HB` / `KB` | читання / запис | Смуга holdback, `1…9999`. Якщо \|PV − поточна SV\| більша за `Hb`, програма **сама** стає на паузу, поки PV не наздожене. Щоб вимкнути — поставити дуже велике значення. Це не `HB` (`48 42`). |
| `Lc` | `4C 63` | `LC` | читання / запис | Лічильник циклів програми: `1…200` або безперервно. Під час RUN показує, скільки ітерацій **залишилось** (включно з поточною). |
| `ch` | `63 68` | `PTN` | читання / запис | Номер патерну `0…9`. Немає в PDF AL808; є в таблиці комунікацій PC900 («程序编号»). Підтверджено на ACHI IR6500 (COM1, 2026-09-18): запис `0…9` = `ACK` і перемикає банк `r/l/t`; `10` = `NAK`. **Не плутати** з `CH` (`43 48`, час циклу нагріву). У RUN/HOLD не змінюється. |
| `SE` | `53 45` | `STEP` | **тільки читання** | Номер поточного сегмента програми. У IDLE на IR6500 повертає `0`. |
| `r1` | `72 31` | `R1` | читання / запис | Швидкість рампу 1, `0.01…99.99` од./хв (або сек, якщо `PRTN = SEC`). Спецзначення на панелі: `END` — завершити програму на цьому рампі; `STEP` — пропустити рамп і перейти до dwell. |
| `l1` | `6C 31` | `L1` | читання / запис | Цільова ставка рампу 1 (level), у межах `SPL…SPK`. |
| `t1` | `74 31` | `D1` | читання / запис | Витримка (dwell) 1, `0…9999` хв. `0` = сегмент пропускається. Під час цього dwell показує **залишок** часу. |
| `r2` | `72 32` | `R2` | читання / запис | Швидкість рампу 2. |
| `l2` | `6C 32` | `L2` | читання / запис | Цільова ставка рампу 2. |
| `t2` | `74 32` | `D2` | читання / запис | Витримка 2. |

Порядок виконання: `R1 → D1 → R2 → D2 → …`  
Старт рампу — від **поточного PV**, не від нуля.

Обмеження з інструкції PC900 (не коди протоколу, але впливають на запис):

- У стані **RUN** параметри програми `LC, R*, L*, D*` з панелі не змінюються.
- У **HOLD** зміна цих параметрів діє лише на **поточну** ітерацію.
- У **IDLE** зміни постійні.
- Номер програми `PTN` не змінюється в RUN/HOLD.

### 4.6. Статус-слова

Формат даних: `>ABCD` (чотири hex-цифри з префіксом `>`).

| C1 C2 | HEX | Доступ | Опис |
|---|---|---|---|
| `SW` | `53 57` | змішаний | Основне статус-слово, див. §5. |
| `XS` | `58 53` | читання / запис | Self-tuning. |
| `OS` | `4F 53` | читання / запис | Керування програмою. |

#### `XS` — автоналаштування PID

| Значення | Дія |
|---|---|
| `>0000` | Вимкнути self-tuning |
| `>0001` | Увімкнути self-tuning |

На панелі це `TUNE` (`OFF`/`ON`). Не запускати під час рампу програми: спочатку `IDLE` або `HOLD`. Self-tune дає термічний удар по об’єкту.

#### `OS` — стан програматора

| Значення | Дія на панелі | Опис |
|---|---|---|
| `>0000` | `IDLE` | Стоп. Працює як звичайний регулятор зі ставкою на нижньому індикаторі. Програма не виконується. Скидання програми на початок. |
| `>0002` | `RUN` | Запуск. З `IDLE` завжди стартує **спочатку**. |
| `>0003` | `HOLD` | Пауза: SV заморожена, таймер сегмента стоїть. Далі можна `RUN` (продовжити) або `IDLE` (скинути). |

Це офіційний спосіб керувати програмою по COM (PC900 §12.4).  
Клеми 14–15 (Run/Stop) мають той самий пріоритет: **остання дія перемагає**.

---

## 5. Розбір `SW` (status word)

Формат: `>ABCD`.  
PDF нумерує біти 0…15 так: нібль **D** = біти 0–3, **C** = 4–7, **B** = 8–11, **A** = 12–15.

Тобто hex-запис `>ABCD` — це ніблі від старшого `A` до молодшого `D`.

| Біт | Нібль | Функція | Доступ | Значення |
|---|---|---|---|---|
| 0 | D0 | Формат даних | читання / запис | `0` = вільний, `1` = фіксований |
| 1 | D1 | Помилка входу / датчика | тільки читання | ні / так |
| 2 | D2 | Блокування клавіш | читання / запис | ні / так |
| 3 | D3 | *(зарезервовано)* | — | — |
| 4 | C0 | *(зарезервовано)* | — | — |
| 5 | C1 | Зміна параметрів з клавіш | читання / запис | ні / так |
| 6 | C2 | Статус аварії відхилення | тільки читання | ні / так |
| 7 | C3 | Умова аварії відхилення | тільки читання | ні / так |
| 8 | B0 | Статус аварії нижньої межі | тільки читання | ні / так |
| 9 | B1 | Умова аварії нижньої межі | тільки читання | ні / так |
| 10 | B2 | Статус аварії верхньої межі | тільки читання | ні / так |
| 11 | B3 | Умова аварії верхньої межі | тільки читання | ні / так |
| 12 | A0 | Вихід аварії (реле) | тільки читання | ні / так |
| 13 | A1 | *(зарезервовано)* | — | — |
| 14 | A2 | *(зарезервовано)* | — | — |
| 15 | A3 | Auto / Manual | тільки читання | auto / manual |

«Статус» і «умова» аварії в PDF розділені: умова — логіка уставки, статус — фактичний стан з урахуванням гістерезису / блокування.

Запис у `SW` має сенс лише для бітів з доступом R/W (формат даних, блокування клавіш, дозвіл зміни з панелі). Решту бітів змінювати не потрібно.

---

## 6. Чого в протоколі немає

| Запит | Результат |
|---|---|
| Команда «яка модель?» | Немає. Модель видно на дисплеї ~3 с після живлення (верхній рядок — код, нижній — версія ПЗ). |
| Читання `ADDR` / `BAUD` / `SN` / `CTRL` | У таблиці AL808 цих мнемонік немає. Це конфігурація з панелі. |
| `NAK` на читання | Немає: невалідне читання = тиша. |
| Підтвердження читання `ACK` | Немає: успіх = кадр `[STX]…[ETX] BCC`. |

---

## 7. Готові кадри для нашого приладу

Умови: адреса **01**, 9600 7E1, COM1.

Читання (безпечно):

| Параметр | HEX запиту |
|---|---|
| `PV` виміряне | `04 30 30 31 31 50 56 05` |
| `OP` вихід % | `04 30 30 31 31 4F 50 05` |
| `SP` ставка | `04 30 30 31 31 53 50 05` |
| `SW` статус | `04 30 30 31 31 53 57 05` |
| `OS` стан програми | `04 30 30 31 31 4F 53 05` |
| `XS` self-tune | `04 30 30 31 31 58 53 05` |
| `SL` локальна ставка | `04 30 30 31 31 53 4C 05` |

Запис — лише з обрахованим BCC по фактичному `DATA`. Без BCC прилад відповість `NAK` або промовчить. Не писати `SP` (read-only): для ставки використовувати `SL`. Не вмикати `XS=>0001` і не змінювати `OS`, якщо не треба рухати програму.

---

## 8. Мінімальний алгоритм драйвера

1. Відкрити порт **7 data, even parity, 1 stop**, потрібний baud.
2. Надіслати poll `PV`.
3. Читати до `ETX` + 1 байт BCC (не зупинятись, якщо трапився байт `04`).
4. Перевірити echo `C1 C2` і BCC.
5. Писати тільки після успішного читання того ж параметра (щоб знати формат `DATA`).
6. Після запису чекати один байт: `06` = OK, `15` = відмова.
7. Таймаут без відповіді: інша адреса, інший baud, або кадр відхилено.

Джерела: ALTEC AL808 Communication Protocol; ALTEC PC900 Instruction Manual, §5 (клеми), §8 (`ADDR`/`BAUD`), §12.4 (керування програмою через статус-слово AL808).
